import { ReviewFilterPicker } from "../ReviewFilterPicker";

export default function SlangReviewPage() {
  return (
    <main className="min-h-screen bg-[var(--color-background)]">
      <ReviewFilterPicker isSlang />
    </main>
  );
}
