# dependencies
node_modules/
.pnp
.pnp.js

# Next.js build output
.next/
out/

# environment variables — never commit real secrets
.env.local
.env.*.local

# TypeScript incremental build info
*.tsbuildinfo
tsconfig.tsbuildinfo

# OS
.DS_Store

# WAL files only — cedict.db is committed so Vercel can deploy it
data/cedict.db-shm
data/cedict.db-wal

# Supabase local dev
supabase/.branches
supabase/.temp
